function count(source, char)
{
	var counter = 0;
	var iterator = 0;

	loop {
		if (source[iterator] == ";")
			break;

		if (source[iterator] == char)
			counter += 1;

		iterator += 1;
	}

	return counter;
}

function say(value) {
	print value;
}

function main()
{
	2 + 90:say();
}